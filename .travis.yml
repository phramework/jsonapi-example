language: php
php:
  - 5.6
  - 7.0
install:
 - composer self-update
 - composer install --no-interaction --dev
before_script:
  - mkdir -p build/
  - mkdir -p build/logs
  - php ./tools/CreateDatabase.php
script:
 - composer test
 - composer testphase
after_script: